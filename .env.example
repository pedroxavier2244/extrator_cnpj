# Copie este arquivo: cp .env.example .env

# --- Obrigatorio ---
DATABASE_URL=postgresql+psycopg2://cnpj_user:senha@localhost:5432/cnpj

# --- Caminhos ETL ---
RAW_DATA_PATH=data/raw
STAGING_PATH=data/staging
PROCESSED_PATH=data/processed
BATCH_SIZE=30000
ETL_HASH_ALGORITHM=sha256

# --- API ---
API_V1_PREFIX=/api/v1
APP_NAME=Sistema CNPJ
LOG_LEVEL=INFO
CORS_ORIGINS=["*"]

# --- Pool de conexoes ---
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=40
DB_POOL_TIMEOUT=30

# --- Cache Redis (opcional — deixe vazio para desabilitar) ---
REDIS_URL=
CACHE_TTL_SECONDS=86400
BATCH_MAX_SIZE=1000
